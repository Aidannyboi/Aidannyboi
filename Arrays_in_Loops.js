var kids = ["Thomas","Cindy","Billy","Mandy"];              //Adds and array of all the kids names.

var age = [8, 8, 9, 7];                                     //Adds an array of all the set kids ages.

for(var x = 0; x <= kids.length - 1; x++){                  //loop for the legnth of the list and after every loop add 1 to X.
console.log(kids[x] + " is now " + age[x] + " years old.")  //tells the log which kid is how old.
}
